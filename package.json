{
    "name": "pgdb",
    "version": "1.0.0",
    "description": "postgres database interface",
    "type": "module",
    "license": "private",
    "private": true,
    "main": "index.js",
    "scripts": {
        "test": "vitest --no-file-parallelism --maxWorkers=1",
        "test:all": "vitest run --no-file-parallelism --maxWorkers=1",
        "test:happy": "vitest run --no-file-parallelism --maxWorkers=1 \"test/01 -test_db_HappyPath.test.js\"",
        "test:Members": "vitest run --no-file-parallelism --maxWorkers=1 \"test/02 -test_db_MemberFunctions.test.js\"",
        "test:trusts": "vitest run --no-file-parallelism --maxWorkers=1 \"test/03 -test_db_TrustFunctions.test.js\""
    },
    "dependencies": {
        "dotenv": "^17.4.2",
        "pg": "^8.23.0",
        "solid-js": "^1.9.15"
    },
    "engines": {
        "node": ">=24"
    },
    "devDependencies": {
        "vitest": "^5.0.0"
    }
}
